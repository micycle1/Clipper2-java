/**
 * This unit contains the ClipperOffset class that performs all polygon
 * offsetting. Nevertheless, the vast majority of offset operations can be
 * performed using the simple InflatePaths function that's found in the Clipper
 * Unit.
 */
package clipper2.offset;